let ff = lam ieio . {lam x . if (isZero x) then true else ((second ieio) (pred x)),
      lam  x . if (isZero x) then false else ((first ieio) (pred x))} in
let tuple = fix ff in 
let isEven = first tuple in 
let isOdd = second tuple in 
let one = succ (zero) in 
let two = succ one in 
let three = succ two in 
isOdd two